#include <iostream>

int main() {
    int number;
    std::cout << "Enter an integer: ";
    std::cin >> number;
    std::cout << (number % 3 == 0 ? "Divisible by 3." : "Not divisible by 3.") << std::endl;
    return 0;
}
